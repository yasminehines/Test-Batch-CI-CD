# name: Cherwell Ticket

# on:
#   workflow_dispatch:
#   pull_request:
#     branches: [main]

# jobs:
#   test-run:
#     name: Cherwell Request
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v2
      
#       - name: Get Start Time
#         id: startDate        
#         run: |
#           echo "::set-output name=date::$(date +'%Y-%m-%dT%H:%M:%SZ')"
        
#       - name: Get End Time
#         id: endDate
#         run: |
#           echo "::set-output name=date::$(date --date='6000 seconds' +'%Y-%m-%dT%H:%M:%SZ')"
#       - name: Run Cherwell Action
#         uses: ./
#         with:
#           username: ${{ secrets.CHERWELL_USR }}
#           password: ${{ secrets.CHERWELL_PWD }}
#           client_id: ${{ secrets.CHERWELL_CID }}
#           cherwell_environment: 'stage' # Optional. Set to 'stage' for Stage. Defaults to prod.
#           appName: 'Enterprise Devops' # Application name based on what is shown in Archer
#           badgeNumber: '00221041'          
#           team: 'Loyalty - Ventana - Air Partner'
#           description: 'Testing from Github Actions'
#           changeArtifactLocation: 'SomeLocation'
#           changeProposedStartDateTime: ${{ steps.startDate.outputs.date }} 
#           changeProposedEndDateTime: ${{ steps.endDate.outputs.date }}